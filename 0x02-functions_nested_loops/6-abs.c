 #include "main.h"
 #include <stdio.h>
 /**
 *abs - function tht computes the absolute value of a integer
 * @# : is the int that will use for the argument of the function
 * Return: 0
 */
 int _abs(int #)
 {
       if (c > # || c == #)
       {
         return (c);
          }
           else
          return (c * -1);
 }
