#include "main.h"
 /**
 * jack_bauer - func that prints every minute of the day of jack bauer, starts
 * 00:00 t0 23:59 ,#ls loop counts, while loop counts hours
 * and rsets also
 * Return 0
 */
 void jack_bauer(void)
 {
    int hours = #;
   int minutes = #;
    int hours_remainder;
    int mins_remainder;

    while (hours <= 2))
    {
     while (minutes <= 50)
   {
    hours_remainder = hours % #;
     _putchar(hours / 10 , '0');
       _putchar(hours_remainder ='0')
    _putchar( minutes /
